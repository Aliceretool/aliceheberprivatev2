customLibraries:
  array:
    - object:
        version: 18.0.1
        language: javascript
        codeString: ''
        libraryName: joi
    - object:
        version: 3.4.0
        language: javascript
        codeString: |
          /* Edit library variable below */

          const uuid = require('uuid')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: uuid
    - object:
        version: 4.17.21
        language: javascript
        codeString: |
          /* Edit library variable below */

          const _ = require('lodash')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: lodash
    - object:
        version: 2.1.0
        language: javascript
        codeString: |
          /* Edit library variable below */

          const numbro = require('numbro')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: numbro
    - object:
        version: 5.3.2
        language: javascript
        codeString: |
          /* Edit library variable below */

          const Papa = require('papaparse')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: papaparse
    - object:
        version: 0.5.23
        language: javascript
        codeString: |
          /* Edit library variable below */

          const moment = require('moment-timezone')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: moment-timezone
name: lvmh repro
setupScripts:
  object:
    python:
      object:
        codeString: ''
    javascript:
      object:
        codeString: |
          // lodash
          /* Edit library variable below */

          const _ = require('lodash')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // numbro
          /* Edit library variable below */

          const numbro = require('numbro')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // papaparse
          /* Edit library variable below */

          const Papa = require('papaparse')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // moment-timezone
          /* Edit library variable below */

          const moment = require('moment-timezone')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // uuid
          /* Edit library variable below */

          const uuid = require('uuid')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
hasProtectedTriggers: true
javascriptLanguageConfiguration:
  object:
    id: 93163582-44a2-4b0a-9280-032cb180dbab
    name: workflow_javascript_3f112e63-da78-42ab-81fe-071878c34a44
    description: null
    libraries:
      object:
        joi:
          object:
            specVersion: 18.0.1
            actualVersion: ''
        uuid:
          object:
            specVersion: 3.4.0
            actualVersion: ''
        lodash:
          object:
            specVersion: 4.17.21
            actualVersion: ''
        numbro:
          object:
            specVersion: 2.1.0
            actualVersion: ''
        papaparse:
          object:
            specVersion: 5.3.2
            actualVersion: ''
        moment-timezone:
          object:
            specVersion: 0.5.23
            actualVersion: ''
    librariesFormat: package.json
    language: javascript
triggerWebhooks:
  array:
    - object:
        name: startTrigger
        uuid: startTrigger
        headers: '{}'
        inputSchema:
          object:
            properties:
              array: []
        useHeaderApiKey: false
        exampleInputJSON: |+
          {
            "id": 100,
            "status": "test",
            "approver": "nobody"
          }


        examplePathParams: ''
version: 43.0.9
workflowId: 3f112e63-da78-42ab-81fe-071878c34a44
description: null
workflowTemplate:
  version: 3.302.0
